import styled from '@emotion/styled';

import { LibraryEntryCard } from '../../../components';

export const Root = styled.div``;

export const List = styled.ul``;

export const ListItem = styled.li``;

export const Card = styled(LibraryEntryCard)``;
